import sectionConfig from './sectionConfig'
import basicConfig from './basicConfig'

// 扩展的基础配置，包含section字段
export const extendedBasicConfig = [
  basicConfig,
  sectionConfig
]

export default extendedBasicConfig
